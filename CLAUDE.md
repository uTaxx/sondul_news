# 손덜 키워드 뉴스 — 작업 지침

이 저장소에서 일할 때 먼저 읽는다.
사용자가 지적한 것들을 그때그때 여기에 쌓는다.

---

## 1. 한국어

### 영어를 직역하지 않는다

가장 자주 지적받은 부분이다. 영어 업계 용어를 그대로 옮기면
어색한 정도가 아니라 **뜻이나 태(voice)가 틀리는 일이 잦다.**

| 쓰지 말 것 | 왜 | 대신 |
|---|---|---|
| 막고 있는 것 (blocker) | 막는 주체가 우리가 돼버린다. 우리가 그것에 막혀 있는 것이지 우리가 무언가를 막는 게 아니다 | 먼저 풀어야 할 것 · 남은 것 · 걸림돌 |
| 열려 있는 것 (open items) | 한국어에서 "열려 있다"는 문이나 가게에 쓴다 | 확인이 필요한 것 · 아직 못 정한 것 · 남은 것 |

새 표현을 쓰기 전에 한 번 묻는다 — **한국말로 원래 그렇게 말하나?**
영어 문서를 옮긴 냄새가 나면 안 쓴다.

### 표기

- **섹터** (○) / 섭터·셉터 (✕)

### 문장

- 명령형·제언형보다 사실을 그대로 적는다
- 과장하지 않는다. 확인한 것과 확인하지 않은 것을 구분해서 쓴다
- 코드 주석은 *무엇을* 하는지가 아니라 *왜* 그렇게 했는지를 적는다

---

## 2. 이 저장소의 원칙

### 공개 저장소다

비밀값을 두지 않는다. 마스터 비밀번호, 고객 접근토큰, API 키, 봇 토큰은
**절대 파일에 적지 않는다.** 전부 구글 시트(`운영설정`)나 n8n 자격증명에 있다.
문서에는 "어디에 있다"까지만 적는다.

### 회사 시스템과 완전히 분리한다

사내용 **LX Group MI**(`LXGroup_MI`, `LXGroup_MI_Sub`, `LXGroup_신청접수`)와
구글 시트·드라이브·워크플로·웹훅·텔레그램 봇이 전부 별개다.

- 회사 워크플로를 **읽는 것은 되고 고치는 것은 안 된다**
- 회사 드라이브 트리(`LX Group MI/`)에 파일을 만들지 않는다.
  고객 시트는 `손덜 키워드 뉴스` 폴더에 둔다
- 복사해 온 코드에 회사 흔적(`LX`, 하드코딩된 시트 ID, 회사 색상 `#A32B26`)이
  남아 있는 경우가 많다. 새 기능을 붙이기 전에 먼저 훑는다

### 대시보드는 회사용과 다르게 보여야 한다

같은 사람이 만든 티가 나지 않게 한다는 것이 처음부터의 요구였다.
색·서체·구성 요소를 회사용에서 가져오지 않는다.

### 브랜드 (2026-09 확정)

손덜랩 브랜드 가이드는 **무채색 여섯 개가 전부이고 포인트 색이 없다.**

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| `--ink` | `#111111` | 본문, 채운 버튼, 로고 |
| `--ink-2` | `#333333` | 부제, 라벨 |
| `--muted` | `#666666` | 보조 설명, 날짜 |
| `--line` | `#E5E5E5` | 선, 테두리 |
| `--bg` | `#F7F7F7` | 바탕, 안쪽 블록 |
| `--card` | `#FFFFFF` | 카드 |

서체는 `Pretendard` 우선, 없으면 `Noto Sans KR`.

로고는 소문자 워드마크 `sondullab` 에 **'s' 아래 2px 밑줄**, 그리고 그 뒤로
**흐려지며 이어지는 점 세 개**다. 밑줄은 지금 하는 일이고 점은 아직 오지 않은 일이라,
홈페이지의 "01 · 빈 02 칸"과 같은 말을 로고 안에서 한다.

- 밑줄 너비 = 글자 크기 × 0.5 (24px → 12px). 'ｓ' 한 글자 폭이다
- 점은 밑줄 끝에서 5px 떨어져 시작, 2px 점 + 5px 간격, 오른쪽으로 마스크를 씌워 흐려진다
- 색은 `currentColor` 로 둔다 — 흰 바탕에서도 검은 띠 위에서도 같은 로고를 쓴다
- 지금 크기: 대시보드 24px, 홈페이지 데스크톱 23px / 모바일 20px

이 2px 막대는 화면 안에서 반복해 쓴다 — 대시보드 좌측 네비의 현재 위치 표시가 같은 막대다.

**상태는 색으로 구분하지 않는다.** 채움 · 테두리 · 굵기로 만든다.

- 정상 → 회색 태그 (눈에 띄지 않아야 한다)
- 임박 → 흰 바탕 + 검정 테두리
- 문제 → 붉은 채움
- 기사 방향 → `▲ 긍정` / `▼ 부정` (색이 아니라 글자로)
- 운영중/멈춤 → 채운 점 / 빈 점

색을 쓰지 않는 이유는 취향이 아니다. 흑백으로 인쇄해도, 색을 구별하기 어려운
사람이 봐도 같은 뜻이 전달돼야 하기 때문이다.

유일한 예외가 `--warn: #B42318` 이다.
**그냥 두면 손해가 나는 것에만** 쓴다 — 만료된 구독, 상한 초과, 마이너스 마진, 삭제.
"좋다"는 뜻의 초록은 두지 않는다. 정상은 조용한 것이 맞다.

### 노드 이름 (2026-09 정리)

**노드 이름은 그 노드가 무슨 일을 하는지 우리말로 읽히게 쓴다.**
n8n 화면에서 이름이 곧 설명이라, 이름이 안 읽히면 나중에 아무도 못 고친다.

쓰지 않는 것

| 안 씀 | 왜 |
|---|---|
| `조립` `게이트` `마커` `분기` `파싱` `되쓰기` | 우리말에서 안 쓰는 말이거나 조어다 |
| `코드파싱3` `설정조회6` `고객확인4` | 번호는 아무것도 알려주지 않는다 |
| `Webhook4` `Respond to Webhook13` `Loop Over Items` | 손댈 때마다 다시 읽어야 한다 |
| `설정행조립` 처럼 붙여 쓴 조어 | 띄어 쓴다 |

쓰는 것

- 하는 일을 그대로: `저장할 내용 만들기`, `고객 목록 읽기`, `기사 지우기`
- 갈림길은 물음으로: `AI를 쓰는 고객인가`, `지울 것이 있나`, `암호가 맞나`
- 같은 단계가 여러 흐름에 있으면 앞에 흐름 이름을 붙이고 `·` 로 나눈다:
  `기사 목록 · 코드 확인` / `지금 조회 · 코드 확인`
- 짝이 있으면 괄호로: `구글 뉴스 가져오기` / `구글 뉴스 가져오기 (영문)`

**이름은 절대 하나씩 바꾸지 않는다.** `$('노드명')` 참조와 연결 정보가 끊어진다.
`TMP_손덜_문자열패치` 로 워크플로 전체를 문자열로 놓고 **따옴표째** 바꾼다
(`"이름"` 과 `'이름'` 둘 다). 따옴표를 붙여야 `운영값` 을 바꿀 때 `운영값2` 가 안 망가진다.
바꾸기 전에 새 이름이 겹치는지 보고, 바꾼 뒤에 연결과 `$()` 참조가 다 살아 있는지 확인한다.

**한글을 `\uXXXX` 로 쓰지 않는다.** 두 번 틀렸다 (`손덩`, `어꺋난다`).
꼭 써야 하면 파이썬으로 코드포인트를 먼저 확인한다.

---

## 3. 돈 쓰는 곳

**무료(공개 채널)에는 클로드 API를 쓰지 않는다.** 수집·묶기·선별은 전부 규칙으로 한다.
유일한 예외가 앞머리 "오늘의 세 줄"인데, 이미 고른 세 건의 제목만 보내므로 월 60원 수준이다.

비쌌던 것은 AI 자체가 아니라 **기사 전수를 평가하는 일**이었다.
AI를 붙일지 말지 판단할 때 이 구분을 먼저 한다.

---

## 4. 되풀이해서 걸렸던 함정

### n8n

- **고치고 나면 반드시 publish 한다.** `update_workflow`는 초안만 바꾼다.
  웹훅은 발행된 판을 쓰므로, publish 전에는 옛 코드가 돈다
- 구글 시트에 쓸 때 **`cellFormat: RAW`**. 기본값(USER_ENTERED)이면
  `07:00`을 시각으로 해석해 `7:00`으로 바꿔 저장한다. 마스터는 `'07:00'`과
  글자 그대로 비교하므로 그 고객만 조용히 발송에서 빠진다
- 텔레그램 노드의 `appendAttribution` 기본값이 **true**다.
  끄지 않으면 고객 메시지 끝에 n8n 홍보문구가 붙는다
- 노드 이름을 바꿀 때는 `$('노드이름')` 참조까지 같이 바꾼다.
  워크플로 JSON을 통째로 문자열 치환하면 한 번에 맞는다
- 실행할 때마다 중복제거가 걸리므로 **같은 날 여러 번 테스트하면 재료가 마른다**.
  두 번째 실행부터는 신규 기사만 남는다
- 구글 시트 노드에 `mappingMode: 'defineBelow'`를 쓰면 **`columns.schema`까지 같이
  줘야 한다.** 안 주면 실행이 통째로 죽는다. 앞 노드가 컬럼명과 똑같은 이름으로
  필드를 내보내면 `autoMapInputData` + `value: {}` 쪽이 짧고 덜 깨진다
- **n8n 공개 API는 실행 총 개수를 주지 않는다.** `data`와 `nextCursor`뿐이다.
  대신 **실행 ID가 빈틈없이 연속**이라 ID 차이가 곧 실행 수다
  (2026-09-02에 250건·513건 두 번 실측해 맞는 것을 확인했다).
  단 n8n은 오래된 실행을 지운다 — 지금 보관은 **일주일쯤(1,150건)**이다.
  달이 길어지면 앞부분이 사라지므로, 속도는 달 누적이 아니라
  **남아 있는 창 안에서** 재야 조용히 낮아지지 않는다
- 워크플로 목록(`resource: workflow, getAll`)은 **노드 정의를 통째로 실어 온다.**
  108개에 1.5MB다. 이름만 필요해도 이만큼 든다는 것을 계산에 넣는다

### 텔레그램

- 권한은 **`callback_query.from.id`**로 판단한다. `chat.id`가 아니다.
  같은 방에 있는 다른 사람도 버튼은 볼 수 있다
- 메시지에 넣는 제목·매체명·사용자 입력은 **HTML 이스케이프**한다.
  `&`나 `<`가 하나 섞이면 메시지 전체가 거절된다
- 개인 대화만 처리할 때는 `chat.type === 'private'`을 확인한다.
  안 그러면 관리자 방에서 우리끼리 한 말에 봇이 끼어든다
- 한 사람에게 못 보냈다고 나머지 발송이 멈추면 안 된다 →
  발송 노드에 `onError: continueRegularOutput`

### 비용 기록

- API가 돌려주는 모델명에는 날짜가 붙는다(`claude-haiku-4-5-20251001`).
  단가표에서 찾을 때 날짜를 뗀다
- 표에 없는 모델이면 **다른 단가로 때우지 않는다.** 0으로 두고 경고를 남긴다.
  틀린 금액을 그럴듯하게 남기면 아무도 눈치채지 못한 채 그 숫자로 판단하게 된다

### 침묵

아침에 아무것도 안 나가면 **죽은 채널로 보인다.** 침묵은 고장과 구분되지 않는다.
고를 것이 없으면 기준을 낮추거나, 최소한 "조용한 날입니다" 한 줄이라도 내보낸다.

---

## 5. 일하는 방식

- **실행 전에 확인한다.** 실제 발송·과금·시트 쓰기가 일어나는 일은 먼저 알린다
- 고친 것은 **실제로 돌려서 확인하고**, 확인한 것과 못 한 것을 나눠서 보고한다
- 커밋 메시지는 한국어로, *무엇을 바꿨는지*보다 *왜 그래야 했는지*를 적는다

---

## 6. 구조 (2026-09 기준)

### n8n (`sondullab.app.n8n.cloud`)

| 워크플로 | 하는 일 |
|---|---|
| `SondulNews_Master` | 매시 고객 목록을 훑어 발송 대상을 고른다 — **아직 꺼져 있다** |
| `SondulNews_Sub` | 수집 → 중복제거 → (AI 평가) → 저장 → 텔레그램·메일 발송 |
| `SondulNews_신청접수` | 웹훅 11개. 고객 화면과 관리자 화면이 부르는 창구 |
| `SondulNews_구독관리` | 만료 알림, 입금·연장 버튼, 봇 DM 창구 |
| `SondulNews_한도` | 웹훅 하나(`sondul-usage`). 관리자 화면 '한도' 탭이 부른다 |
| `TMP_손덜_문자열패치` | 워크플로 문자열을 통째로 치환하는 도구. 평소엔 연결을 끊어둔다 |

**`SondulNews_한도`를 왜 따로 뒀나.** 이 창구는 n8n 실행 목록(천 건대)과
워크플로 목록(108개 1.5MB)을 함께 든다. 신청접수(노드 120개) 안에 넣었더니
웹훅 작업자가 **1.7초 만에 메모리로 죽었다.** 무거운 목록을 드는 웹훅은
큰 워크플로에 얹지 않는다. 성공한 실행은 데이터를 안 남기게 해뒀다
(`saveDataSuccessExecution: none`) — 안 그러면 부를 때마다 1.5MB가 실행 기록에 쌓인다.

### 구글 시트 — `설정` 문서 한 개

| 탭 | 내용 |
|---|---|
| 설정 | 고객 한 명당 한 줄 |
| 비용 | API 호출 기록 |
| 운영설정 | 마스터 비밀번호·방 번호, 요금제 상한과 월요금, 만료 알림 기준, 입금계좌, 체험 일수 |
| 지표마스터 / 지표값 | 시장지표 (아직 안 씀) |

한도 값(`n8n_월한도`, `네이버_일한도`, `네이버_월한도`, `claude_월예산USD`)도
`운영설정`에 있다. 코드에는 기본값만 두고, 바꿀 일이 생기면 시트에서 바꾼다.

`운영설정`의 **`입금계좌`는 만료 안내에 그대로 붙는 한 줄**이다
(예: `국민 123456-78-901234 (홍길동)`). 비어 있으면 구독관리가 계좌 줄을 통째로
빼고 보내므로, 고객은 "돈을 보내라"는 말만 받고 어디로 보낼지는 못 듣는다.
결제를 받기 시작하기 전에 반드시 채운다.

고객 행의 `결제상태`가 계정 성격을 가른다.

- `정상` — 유료
- `체험` — 무료 체험. 요금 0으로 집계
- `공개` — 공개 채널. 요금 0, 만료 알림 제외, 만료일 2099-12-31
- `만료` — 기간 지남

### 화면

`index.html`(고객) · `admin.html`(마스터) · `hub.html`(알림 링크 스텁).
고객 링크는 `index.html#t=접근토큰` 꼴이다. 토큰을 쿼리스트링에 두지 않는다 —
Referer 헤더에 실려 언론사 서버로 넘어간다.

`admin.html`은 탭 셋이다 — 고객 · API 비용 · 한도.
**한도 탭의 숫자는 화면에서 계산하지 않는다.** 서버가 만든 것을 그대로 그린다.
같은 값을 두 곳에서 따로 세면 반드시 어긋나고, 어긋난 뒤에는 어느 쪽이 맞는지
알 방법이 없다. 추정인 숫자(네이버 호출)는 화면에 추정이라고 적어둔다.
